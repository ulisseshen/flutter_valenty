name: clean_arch_weather_example
description: Valenty clean architecture weather example — demonstrates acceptance testing with domain/data/presentation layers
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  test: ^1.25.0
  valenty_dsl:
    path: ../../packages/valenty_dsl
