name: legacy_todo_app_example
description: Valenty legacy app example — singleton testing with @visibleForTesting factory pattern
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  meta: ^1.12.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
  valenty_dsl:
    path: ../../packages/valenty_dsl
