name: auth_flow_example
description: Valenty auth flow example with SecureStorage, Dio, SharedPreferences
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  http: ^1.2.0

dev_dependencies:
  test: ^1.25.0
  valenty_dsl:
    path: ../../packages/valenty_dsl
